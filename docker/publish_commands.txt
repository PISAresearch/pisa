0. Still need to check all the correct packages imports, and do a proper hoist


0. lerna clean
0. leran install - bootstrap
0. lerna build - jus to check that all works

1. lerna version --no-git-tag-version
1. 0 git commit -m "Version bump"
2. lerna publish from-package --dist-tag dev
3. lerna exec \"npm i --package-lock-only"
3. 0 git commit -m "Version bump package locks"
4. git tag